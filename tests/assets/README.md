# Rive test assets

- `state_machine_test.riv` is a tiny regression asset generated from the MIT-licensed
  `stateMachineFileBytes` fixture in `rive-app/rive-wasm`:
  `js/test/assets/bytes.ts`.
- This asset is exercised by both the native `rive-cpp` backend attempt and the explicit Node
  fallback renderer tests in this repository.
- Source repository: https://github.com/rive-app/rive-wasm
- Upstream license: MIT
